import { defineConfig } from 'vite';

// https://vitejs.dev/config/
export default defineConfig({
  // Vite has out-of-the-box support for TypeScript, so no special plugins are
  // needed for this project's setup.
});
